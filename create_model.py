import pandas as pd
import pickle
import numpy as np
import tensorflow as tf


# df =
# X = df['']
# y = df['']

# create tensorflow nn
# fit model

# save model with pickle

# model.save('path/to/location') - for keras model
# model = keras.models.load_model('path/to/location')
